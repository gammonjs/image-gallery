import UsecaseInteractor from './interactor'

describe('UsecaseInteractor', () => {

    it('should call images use case connect when connect is called', () => {
        // arrange
        

        // act

        // assert
    })
});